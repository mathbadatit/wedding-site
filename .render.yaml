services:
  - type: web
    name: wedding-site
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn wedding_site.wsgi
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: weddingdb
          property: connectionString

databases:
  - name: weddingdb
    plan: free
